name: ${CONTROL_HISTORY_POSTGRES_PROJECT:?set CONTROL_HISTORY_POSTGRES_PROJECT}

services:
  postgres:
    image: postgres:18.6-alpine@sha256:d3e1620b530c944afa6e887d22eb899824da68e19c52024bf98f5220c88a65b2
    # Capacity runs seed millions of rows, so bound WAL and force periodic
    # checkpoints; without this the run leaves 6.5-8.8GB of WAL on disk and
    # exceeds the ~14GB GitHub-hosted runner budget before the job finishes.
    command: ["postgres", "-c", "max_connections=16", "-c", "max_wal_size=512MB",
              "-c", "min_wal_size=128MB", "-c", "checkpoint_timeout=60s"]
    environment:
      POSTGRES_DB: relay_station_control
      POSTGRES_USER: relay_control_migrator
      POSTGRES_PASSWORD: relay_control_migrator_dev_only
      TZ: UTC
      PGTZ: UTC
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U relay_control_migrator -d relay_station_control"]
      interval: 1s
      timeout: 2s
      retries: 60
    ports:
      - "127.0.0.1::5432"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
      - SETGID
      - SETUID
    volumes:
      - history-postgres-data:/var/lib/postgresql
      - ../postgres/init:/docker-entrypoint-initdb.d:ro
    networks:
      - isolated
      - host-access

volumes:
  history-postgres-data:

networks:
  isolated:
    name: ${CONTROL_HISTORY_POSTGRES_PROJECT:?set CONTROL_HISTORY_POSTGRES_PROJECT}_isolated
    internal: true
  host-access:
    name: ${CONTROL_HISTORY_POSTGRES_PROJECT:?set CONTROL_HISTORY_POSTGRES_PROJECT}_host-access
