import { useEffect, useMemo, useState } from "react";
import { useQuery } from "@tanstack/react-query";
import { Alert, Button, Card, Descriptions, Empty, Flex, Form, Input, Modal, Popconfirm, Select, Space, Spin, Table, Tag, Typography } from "antd";
import type { ReactNode } from "react";
import { AssetApiError, type AssetApi, type DriverAsset, type DriverScope } from "../api/asset-types";
import type { GatewayAdminApi } from "../api/gateway-api";
import { GatewayApiError } from "../api/gateway-api";
import type { GatewayAsset, GatewayAssetDetailResponse } from "../api/generated/control";
import { buildCredentialPatch } from "./asset-credential";
import { useCurrentProviderPolicy, useDriverAssets, useEnvironmentAsset, useGatewayAsset } from "../api/asset-hooks";
import { formatDateTime } from "../foundation/format";
import { useOptionalAppLocale } from "../foundation/FrontendFoundationProvider";
import { validateInternalHttpEndpoint } from "../validation/internal-http-endpoint";
import { useTranslation } from "react-i18next";
import type { TFunction } from "i18next";

const { Text } = Typography;
type GatewayFormValues = { display_name: string; management_endpoint: string; credential?: string; new_instance_id?: string; credential_action?: "keep" | "set" | "clear" };
export { buildCredentialPatch } from "./asset-credential";

function commandId() { return globalThis.crypto?.randomUUID?.() ?? `${Date.now()}-${Math.random().toString(16).slice(2)}`; }
function capabilities(values: string[]) { return values.length ? <Space size={[0, 4]} wrap>{values.map((value) => <Tag key={value}>{value}</Tag>)}</Space> : "—"; }
function gatewayError(error: unknown, t: TFunction) {
  if (error instanceof GatewayApiError) {
    if (error.code === "stale_revision") return t("assets.errorStale");
    if (error.code === "command_conflict") return t("assets.errorConflict");
    if (error.code === "invalid_endpoint") return t("assets.errorEndpoint");
    if (error.code === "current_gateway_exists") return t("assets.errorGatewayExists");
    if (error.code === "asset_retired") return t("assets.errorRetired");
    if (error.status === 401) return t("assets.errorExpired");
    if (error.status === 403) return t("assets.errorForbidden");
  }
  return t("assets.errorOperation");
}
function ResourceFrame({ loading, error, retry, children, labels }: { loading: boolean; error: unknown; retry(): void; children: ReactNode; labels: { readFailed: string; unavailable: string; retry: string } }) {
  if (loading) return <Flex justify="center" className="asset-loading"><Spin /></Flex>;
  if (error) return <Alert type="error" showIcon message={labels.readFailed} description={labels.unavailable} action={<Button onClick={retry}>{labels.retry}</Button>} />;
  return children;
}

function GatewayManagement({ api, csrfToken, onUnauthorized }: { api: GatewayAdminApi; csrfToken: string; onUnauthorized(): void }) {
  const { t } = useTranslation();
  const locale = useOptionalAppLocale()?.locale ?? "zh-CN";
  const [lifecycle, setLifecycle] = useState<"active" | "retired" | "all">("active");
  const [cursorHistory, setCursorHistory] = useState<Array<string | undefined>>([undefined]);
  const [selected, setSelected] = useState<GatewayAsset>();
  const [detail, setDetail] = useState<GatewayAssetDetailResponse>();
  const [modal, setModal] = useState<"register" | "edit" | "replace">();
  const [busy, setBusy] = useState(false);
  const [message, setMessage] = useState<string>();
  const [form] = Form.useForm<GatewayFormValues>();
  const cursor = cursorHistory.at(-1);
  const list = useQuery({ queryKey: ["asset-registry", "gateways", lifecycle, cursor], queryFn: () => api.list(lifecycle, cursor), retry: false, staleTime: 0 });
  const report = (error: unknown) => { if (error instanceof GatewayApiError && error.status === 401) onUnauthorized(); else setMessage(gatewayError(error, t)); };
  const openForm = (next: "register" | "edit" | "replace", asset?: GatewayAsset) => { setSelected(asset); setModal(next); setMessage(undefined); form.setFieldsValue(asset ? { display_name: asset.display_name, management_endpoint: asset.management_endpoint, new_instance_id: undefined, credential: undefined, credential_action: "keep" } : { display_name: "", management_endpoint: "http://", new_instance_id: "", credential: undefined, credential_action: "set" }); };
  const submit = async (values: GatewayFormValues) => {
    if (!modal) return;
    setBusy(true);
    try {
      if (modal === "register") await api.register({ command_id: commandId(), new_instance_id: values.new_instance_id!, display_name: values.display_name, management_endpoint: values.management_endpoint, ...(values.credential ? { directory_credential: values.credential } : {}) }, csrfToken);
      else if (modal === "edit" && selected) await api.edit(selected.instance_id, { command_id: commandId(), expected_revision: selected.revision, display_name: values.display_name, management_endpoint: values.management_endpoint, ...buildCredentialPatch("directory_credential", values.credential_action, values.credential) }, csrfToken);
      else if (modal === "replace" && selected) await api.replace(selected.instance_id, { command_id: commandId(), expected_revision: selected.revision, new_instance_id: values.new_instance_id!, display_name: values.display_name, management_endpoint: values.management_endpoint, ...(values.credential ? { directory_credential: values.credential } : {}) }, csrfToken);
      setModal(undefined); await list.refetch();
    } catch (error) { report(error); } finally { setBusy(false); }
  };
  const retire = async (asset: GatewayAsset) => { setBusy(true); try { await api.retire(asset.instance_id, { command_id: commandId(), expected_revision: asset.revision }, csrfToken); await list.refetch(); } catch (error) { report(error); } finally { setBusy(false); } };
  const probe = async (asset: GatewayAsset, connectionTest: boolean) => { setBusy(true); try { await (connectionTest ? api.connectionTest(asset.instance_id, csrfToken) : api.health(asset.instance_id)); setMessage(connectionTest ? "Connection Test 已完成。" : "Health 检查已完成。"); } catch (error) { report(error); } finally { setBusy(false); } };
  const items = list.data?.items ?? [];
  return <Card title={t("assets.managementTitle")} data-testid="gateway-management-card"><Flex vertical gap={12}>
    {message && <Alert type="warning" showIcon message={message} closable onClose={() => setMessage(undefined)} />}
    <Flex justify="space-between" align="center" wrap gap={8}><Select data-testid="gateway-lifecycle-filter" aria-label={t("assets.lifecycleFilter")} value={lifecycle} onChange={(value) => { setLifecycle(value); setCursorHistory([undefined]); }} options={[{ value: "active", label: t("assets.currentGateway") }, { value: "retired", label: <span data-testid="gateway-filter-retired-option">{t("assets.retiredGateway")}</span> }, { value: "all", label: t("assets.all") }]} />{lifecycle === "active" && items.length === 0 && <Button data-testid="gateway-register" type="primary" onClick={() => openForm("register")}>{t("assets.registerGateway")}</Button>}</Flex>
    {list.isPending ? <Spin /> : list.error ? <Alert type="error" message={t("assets.listReadFailed")} action={<Button onClick={() => void list.refetch()}>{t("assets.retry")}</Button>} /> : <><Table<GatewayAsset> rowKey="instance_id" size="small" pagination={false} dataSource={items} scroll={{ x: 1100 }} columns={[{ title: t("assets.name"), dataIndex: "display_name" }, { title: "Instance ID", dataIndex: "instance_id", render: (value: string) => <Text code>{value}</Text> }, { title: t("assets.status"), dataIndex: "lifecycle_status", render: (value: string) => <Tag>{value === "active" ? "当前" : "已退役"}</Tag> }, { title: t("assets.revision"), dataIndex: "revision" }, { title: t("assets.credential"), dataIndex: "secret_configured", render: (value: boolean) => value ? t("assets.configured") : t("assets.notConfigured") }, { title: t("assets.operation"), key: "actions", render: (_: unknown, asset: GatewayAsset) => <Space wrap><Button data-testid={`gateway-details-${asset.instance_id}`} onClick={() => void api.detail(asset.instance_id).then(setDetail).catch(report)}>{t("assets.details")}</Button><Button data-testid={`gateway-health-${asset.instance_id}`} onClick={() => void probe(asset, false)} disabled={asset.lifecycle_status !== "active" || busy}>{t("assets.health")}</Button><Button data-testid={`gateway-connection-${asset.instance_id}`} onClick={() => void probe(asset, true)} disabled={asset.lifecycle_status !== "active" || busy}>{t("assets.connectionTest")}</Button>{asset.lifecycle_status === "active" && <><Button data-testid={`gateway-edit-${asset.instance_id}`} onClick={() => openForm("edit", asset)}>{t("assets.edit")}</Button><Button data-testid={`gateway-replace-${asset.instance_id}`} onClick={() => openForm("replace", asset)}>{t("assets.replace")}</Button><Popconfirm title={t("assets.retireGatewayTitle")} onConfirm={() => void retire(asset)} okText={t("assets.confirmRetire")} cancelText={t("assets.cancel")} okButtonProps={{ "data-testid": `gateway-retire-confirm-${asset.instance_id}` }} cancelButtonProps={{ "data-testid": `gateway-retire-cancel-${asset.instance_id}` }}><Button data-testid={`gateway-retire-${asset.instance_id}`} danger loading={busy}>{t("assets.retire")}</Button></Popconfirm></>}</Space> }]} /><Flex justify="end" gap={8}><Button disabled={cursorHistory.length === 1} onClick={() => setCursorHistory((history) => history.slice(0, -1))}>{t("topology.firstPage")}</Button><Button disabled={!list.data?.next_cursor || list.isFetching} onClick={() => list.data?.next_cursor && setCursorHistory((history) => [...history, list.data!.next_cursor!])}>{t("topology.nextPage")}</Button></Flex></>}
    <Modal open={Boolean(modal)} title={modal === "register" ? t("assets.gatewayFormRegister") : modal === "edit" ? t("assets.gatewayFormEdit") : t("assets.gatewayFormReplace")} okText={t("assets.save")} cancelText={t("assets.cancel")} confirmLoading={busy} okButtonProps={{ "data-testid": "gateway-form-submit" }} cancelButtonProps={{ "data-testid": "gateway-form-cancel" }} onCancel={() => setModal(undefined)} onOk={() => void form.submit()} destroyOnHidden><Form form={form} layout="vertical" onFinish={(values) => void submit(values)}>
      {modal !== "edit" && <Form.Item name="new_instance_id" label={t("assets.newInstanceId")} rules={[{ required: true }]}><Input data-testid="gateway-form-instance-id" /></Form.Item>}<Form.Item name="display_name" label={t("assets.displayName")} rules={[{ required: true }]}><Input data-testid="gateway-form-display-name" /></Form.Item><Form.Item name="management_endpoint" label={t("assets.endpoint")} rules={[{ required: true, message: t("assets.validHttp") }, { validator: (_, value) => { const error = validateInternalHttpEndpoint(value); return error ? Promise.reject(new Error(error)) : Promise.resolve(); } }]}><Input data-testid="gateway-form-endpoint" /></Form.Item><Form.Item name="credential" label={t("assets.directoryCredential")} dependencies={["credential_action"]} rules={[({ getFieldValue }) => ({ validator: async (_, value) => { if (modal === "edit" && getFieldValue("credential_action") === "set" && !value) throw new Error(t("assets.requiredCredential")); } })]}><Input data-testid="gateway-credential-input" autoComplete="new-password" /></Form.Item>{modal === "edit" && <Form.Item name="credential_action" label={t("assets.credentialAction")}><Select data-testid="gateway-credential-action" options={[{ value: "keep", label: t("assets.credentialKeep") }, { value: "set", label: t("assets.credentialSet") }, { value: "clear", label: t("assets.credentialClear") }]} /></Form.Item>}
    </Form></Modal>
    <Modal data-testid="gateway-detail-dialog" open={Boolean(detail)} title={t("assets.gatewayDetails")} footer={null} closeIcon={<span data-testid="gateway-detail-close">×</span>} onCancel={() => setDetail(undefined)}>{detail && <Descriptions column={1} size="small" bordered><Descriptions.Item label={t("assets.instanceId")}><Text code>{detail.asset.instance_id}</Text></Descriptions.Item><Descriptions.Item label={t("assets.status")}>{detail.asset.lifecycle_status}</Descriptions.Item><Descriptions.Item label={t("assets.revision")}>{detail.asset.revision}</Descriptions.Item><Descriptions.Item label={t("assets.name")}>{detail.asset.display_name}</Descriptions.Item><Descriptions.Item label={t("assets.endpoint")}><Text code>{detail.asset.management_endpoint}</Text></Descriptions.Item><Descriptions.Item label={t("assets.credential")}>{detail.asset.secret_configured ? t("assets.configured") : t("assets.notConfigured")}</Descriptions.Item><Descriptions.Item label={t("assets.predecessorLabel")}>{detail.predecessor?.old_instance_id ?? "—"}</Descriptions.Item><Descriptions.Item label={t("assets.successorLabel")}>{detail.successor?.new_instance_id ?? "—"}</Descriptions.Item></Descriptions>}</Modal>
  </Flex></Card>;
}

function DriverCatalog({ api, labels }: { api: AssetApi; labels: { readFailed: string; unavailable: string; retry: string } }) { const { t } = useTranslation(); const drivers = useDriverAssets(api); return <Card title={t("assets.drivers")} data-testid="drivers-card"><ResourceFrame loading={drivers.isPending} error={drivers.error} retry={() => void drivers.refetch()} labels={labels}>{drivers.data?.length ? <Table<DriverAsset> rowKey={(driver) => `${driver.nodeType}:${driver.driverContractVersion}`} size="small" pagination={false} dataSource={drivers.data} columns={[{ title: t("assets.nodeTypeLabel"), dataIndex: "nodeType" }, { title: t("assets.contractVersion"), dataIndex: "driverContractVersion" }, { title: t("assets.name"), dataIndex: "displayName" }, { title: t("assets.status"), dataIndex: "status" }, { title: t("assets.capabilities"), dataIndex: "capabilities", render: capabilities }]} /> : <Empty image={Empty.PRESENTED_IMAGE_SIMPLE} description={t("assets.emptyDriver")} />}</ResourceFrame></Card>; }

export function AssetRegistryView({ api, gatewayApi, csrfToken = "", onUnauthorized }: { api: AssetApi; gatewayApi?: GatewayAdminApi; csrfToken?: string; onUnauthorized(): void }) {
  const { t } = useTranslation(); const locale = useOptionalAppLocale()?.locale ?? "zh-CN"; const labels = { readFailed: t("assets.readFailed"), unavailable: t("assets.unavailable"), retry: t("assets.retry") };
  const environment = useEnvironmentAsset(api); const gateway = useGatewayAsset(api); const drivers = useDriverAssets(api); const [policyScope, setPolicyScope] = useState<DriverScope>(); const policy = useCurrentProviderPolicy(api, policyScope);
  useEffect(() => { if ([environment.error, gateway.error, drivers.error, policy.error].some((error) => error instanceof AssetApiError && error.status === 401)) onUnauthorized(); }, [drivers.error, environment.error, gateway.error, onUnauthorized, policy.error]);
  const policyScopes = useMemo(() => (drivers.data ?? []).map((driver) => ({ value: `${driver.nodeType}\u0000${driver.driverContractVersion}`, label: `${driver.nodeType} / ${driver.driverContractVersion}` })), [drivers.data]);
  return <Flex vertical gap={20} data-testid="asset-registry-view">{gatewayApi && <GatewayManagement api={gatewayApi} csrfToken={csrfToken} onUnauthorized={onUnauthorized} />}<div className="asset-card-grid"><Card title={t("assets.environment")} data-testid="environment-card"><ResourceFrame loading={environment.isPending} error={environment.error} retry={() => void environment.refetch()} labels={labels}>{environment.data && <Descriptions column={1} size="small"><Descriptions.Item label={t("assets.environmentId")}><Text code>{environment.data.environmentId}</Text></Descriptions.Item><Descriptions.Item label={t("assets.type")}>{environment.data.environmentType}</Descriptions.Item><Descriptions.Item label={t("assets.name")}>{environment.data.displayName}</Descriptions.Item></Descriptions>}</ResourceFrame></Card><Card title={t("assets.gateway")} data-testid="gateway-card"><ResourceFrame loading={gateway.isPending} error={gateway.error} retry={() => void gateway.refetch()} labels={labels}>{gateway.data?.status === "configured" && gateway.data.gateway ? <Descriptions column={1} size="small"><Descriptions.Item label={t("assets.name")}>{gateway.data.gateway.displayName}</Descriptions.Item><Descriptions.Item label={t("assets.instanceId")}><Text code>{gateway.data.gateway.instanceId}</Text></Descriptions.Item><Descriptions.Item label={t("assets.endpoint")}><Text code>{gateway.data.gateway.managementEndpoint}</Text></Descriptions.Item><Descriptions.Item label={t("assets.credential")}>{gateway.data.gateway.secretConfigured ? t("assets.configured") : t("assets.notConfigured")}</Descriptions.Item><Descriptions.Item label={t("assets.updatedAt")}>{formatDateTime(gateway.data.gateway.updatedAt, locale)}</Descriptions.Item></Descriptions> : <Empty image={Empty.PRESENTED_IMAGE_SIMPLE} description={t("assets.emptyGateway")} />}</ResourceFrame></Card><Card title={t("assets.policy")} data-testid="policy-card"><Flex vertical gap={12}>{policyScopes.length > 0 && <Select data-testid="asset-provider-policy-scope" aria-label={t("assets.providerPolicyScope")} value={policyScope ? `${policyScope.nodeType}\u0000${policyScope.driverContractVersion}` : undefined} options={policyScopes} onChange={(value) => { const next = drivers.data?.find((driver) => `${driver.nodeType}\u0000${driver.driverContractVersion}` === value); if (next) setPolicyScope({ nodeType: next.nodeType, driverContractVersion: next.driverContractVersion }); }} />}<ResourceFrame loading={Boolean(policyScope) && policy.isPending} error={policy.error} retry={() => void policy.refetch()} labels={labels}>{!policyScope ? <Empty image={Empty.PRESENTED_IMAGE_SIMPLE} description={t("assets.selectDriver")} /> : policy.data?.status === "configured" && policy.data.policy ? <Descriptions column={1} size="small"><Descriptions.Item label={t("assets.version")}><Text code>{policy.data.policy.policyVersionId}</Text></Descriptions.Item><Descriptions.Item label={t("assets.scope")}>{policy.data.policy.nodeType} / {policy.data.policy.driverContractVersion}</Descriptions.Item><Descriptions.Item label={t("assets.policyActive")}>{capabilities(policy.data.policy.activeProviders)}</Descriptions.Item><Descriptions.Item label={t("assets.policyOutOfScope")}>{capabilities(policy.data.policy.outOfScopeProviders)}</Descriptions.Item><Descriptions.Item label={t("assets.activeWindow")}>{formatDateTime(policy.data.policy.effectiveFrom, locale)} – {formatDateTime(policy.data.policy.effectiveTo, locale)}</Descriptions.Item></Descriptions> : <Empty image={Empty.PRESENTED_IMAGE_SIMPLE} description={t("assets.emptyPolicy")} />}</ResourceFrame></Flex></Card></div><DriverCatalog api={api} labels={labels} /></Flex>;
}
